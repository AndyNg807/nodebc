let height = 10;
let base = 4;
let perimiter = (height + base) * 2;
let area = height * base;

console.log("The rectangular perimeter is (" + height + " + " + base + ") * 2 and the result is " + perimiter);
console.log("The rectangular area is " + height + " * " + base + " and the result is " + area);