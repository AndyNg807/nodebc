const mutants = ["Professor X", "Cyclops", "Iceman", "Angel", "Magneto", "Beast", "Phoenix", "Logan", "Gambit"];

mutants.forEach(function(each, index) {
    console.log(`name: ${each}  position: ${index}`);
})
